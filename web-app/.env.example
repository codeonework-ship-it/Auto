# AutoHub web-app environment variables.
# Copy to `.env` for local development. Vite only exposes vars prefixed with VITE_.

# Base URL for the backend REST API (versioned).
VITE_API_BASE_URL=http://localhost:8080/api/v1

# App display name (optional).
VITE_APP_NAME=AutoHub
