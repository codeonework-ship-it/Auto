package com.autohub.community.domain.model;

/** Role a user holds within a community group (table {@code community_memberships.role}). */
public enum MembershipRole {
    OWNER,
    MODERATOR,
    MEMBER
}
